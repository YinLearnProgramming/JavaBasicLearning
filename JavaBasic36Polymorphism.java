
public class JavaBasic36Polymorphism 
{
//A superclass reference variable can reference objects of a subclass
	
	//GradedActivity exam1 - new finalExam(SO, 7);
	//GradedActivity exam2 - new PassrailActivity(70);
	//GradedActivity exam3 - new PassFailExam!100, 10, 70);
	//displayGrades(exam1); // Pass a FinalExam object.
	//displayGrades(exam2); // Pass a PassFailActivity object.
	//displayGrades(exam3); // Pass a PaasFailExam object.
	//check object belong to which class ->>> instanceof
}
